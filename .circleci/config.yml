version: 2.1

orbs:
  maven: circleci/maven@1.2.0

jobs:
  build:
    docker:
      - name: orbs
    steps:
      - run: echo "testcontainers.reuse.enable=true" >> testcontainers.properties

workflows:
  maven_test:
    jobs:
      - build
      - maven/test